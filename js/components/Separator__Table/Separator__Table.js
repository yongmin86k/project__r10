import React from 'react';
import {View} from 'react-native';
import {THEME} from '../../config';

const Separator__Table = () => (
  <View
    style={{
      backgroundColor: THEME.color.greyL,
      height: 1,
    }}></View>
);

export default Separator__Table;
